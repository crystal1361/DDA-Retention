# DDA 挽留决策引擎重建 —— 从 0 到 1 完整讲解

写这份文档的目的：不是让你背下来，而是让你真正理解每一步"为什么这么做"，这样面试官不管从哪个角度深挖，你都能顺着逻辑答上去，而不是靠记忆一个数字。建议你先通读一遍，再对照代码（`src/01_generate_data.py` 到 `08_business_impact.py`）看一遍，最后用第7节的自测清单检查自己。

---

## 0. 30 秒电梯陈述（如果面试官只给你一句话的时间）

"我在真实DDA挽留项目基础上，用合成数据重建了一个分析引擎，展示这个岗位要求但我原项目没做到的三件事：在无法随机化的场景下做因果推断（三种不同的方法，因为三个触发器的结构都不一样）、把经验规则换成正式的预算优化、最后把这一切翻译成一份可以量化、可以分优先级执行的业务建议。"

---

## 1. 大局观：这个项目在讲什么故事

### 1.1 真实项目 vs. 这次重建，边界在哪

**真实的（可以理直气壮地说"我做过"）：**
- DDA流失没有预警系统，挽留是无差别的
- 设计了多分类预测模型（预测账户会往哪种方式流失）+ 触发式干预
- 用真实A/B test验证：-30%相对流失
- 原项目Q7里自己承认的gap：验证只靠这一个A/B test结果，没有准实验分析，没有正式优化

**这次重建的（诚实地说"这是我后来在此基础上做的技术展示"）：**
- 用合成数据（内嵌已知真实效应）做的：RDD、客户价值分层DiD、DoubleML、ILP优化、业务影响量化
- 目的很单一：证明我具备这个Vanguard岗位要求、但原项目没有机会展示的技能——准实验因果推断 + 优化 + 业务转化

**为什么要这样切割着说，而不是简单地说"这都是我做的"：** 因为一旦被追问"这个是在公司做的吗"，编造的答案在深挖下必然露馅，而诚实的框定反而是加分项——它展示了你知道"验证过的结论"和"证明了方法论对不对"是两码事，这本身就是这个岗位要考察的分析严谨性。

### 1.2 为什么用合成数据，而不是想办法搞到真实数据

三个原因，按重要性排：
1. 真实账户级数据 + 设计好的准实验结构，不可能带出银行（隐私、合规）
2. 就算能带出来，也不能公开发在GitHub上给面试官看
3. **最关键的一点**：合成数据让我能在生成的时候就内嵌一个"已知的真实效应"（`ground_truth.json`），这样RDD、DiD、DoubleML算出来的数字，我能直接对照真值打分，验证方法本身有没有用对——这是一个真实数据集给不了你的东西。你去信任一个方法之前，得先在你知道答案的地方练过手。

---

## 2. 整体架构：一个决策引擎，四层

```
① PREDICT（预测）      →  ② EXPLAIN（因果解释）    →  ③ OPTIMIZE（优化）   →  ④ ACT（业务转化）
03_predictive_model.py    04/05/07（RDD/DiD/DoubleML）   06_optimization.py    08_business_impact.py
```

每一层解决一个不同的问题：
- **① 预测**：这个账户最可能往哪个方向流失？（不知道"为什么"，只是打分排序）
- **② 因果解释**：如果我对这个账户做某个干预，效果有多大？（这是"预测"给不了的——预测模型看到的是相关性，不是因果）
- **③ 优化**：预算和人力有限，到底该把干预分给谁？（有了②的效应大小，还要考虑成本和约束）
- **④ 业务转化**：这一切值多少钱？该向业务方提什么建议、按什么优先级？（不然前面三层都只是技术展示，不是"对业务有用的分析"）

**这个架构本身就是在回答JD里的话**："design the analytical engine... how relationships are modeled... how outputs are generated for decision-making" ——预测/解释/优化就是这个engine，第④层是把engine的输出真正变成决策。

---

## 3. 逐脚本讲解：每一步为什么这么做

### 3.1 `01_generate_data.py` —— 一切的起点：合成数据 + 内嵌真值

**核心设计：`SEED = 42`，`rng = np.random.default_rng(SEED)`**
为什么要设种子：保证每次重跑生成的是同一份数据，数字可复现——面试官问"这个数能不能复现"，答案是"能，数据生成是seeded的"。

**核心创新点：`account_value` 公式（这是整个项目里被面试官挑战过、后来重新设计的部分）**
```python
PRODUCT_VALUE_UPLIFT = 0.15
def account_value(balance, product_count):
    return balance * (1 + PRODUCT_VALUE_UPLIFT * product_count)
```
- **为什么不用之前那种加权指数**（`product_count*3 + tenure_months*0.05 + balance_tier*2`）：那种公式的权重（3、0.05、2）没有任何来源，面试官一问"为什么是3不是4"就答不上来了。
- **为什么现在这样设计**：改成美元单位——每多一个产品，大致带来15%的增量价值（交叉销售、粘性、更低流失风险）。这样至少每一项都有真实世界的含义，不是凭空拍的指数。
- **15%这个数字本身怎么辩护**：不是靠"这个数字是对的"，而是靠敏感性检验（见`08_business_impact.py`）——换成10%或20%，排序几乎不变（Spearman > 0.999，前25%分位账户98%+不变）。**这是一个非常重要的方法论：当你没法证明一个假设数字"对"的时候，证明结论对这个数字不敏感，这本身就是答案。**

**四份数据集，分别对应四个不同的因果推断场景，这是设计的核心**：

| 数据集 | 用途 | 关键设计 |
|---|---|---|
| `predictive_data.csv`（4万条） | 多分类预测模型训练 | `value_score = account_value(...)` |
| `rdd_data.csv`（1.5万条） | RDD分析 | running variable = 取款占余额的百分比，cutoff=30%，真实效应 `logit=-0.9` |
| `did_data.csv`（1.2万条） | DiD分析 | 按`account_value`四分位分层（tier1_top25pct...tier4_bottom25pct），上线月份 8/14/20/never |
| `dormant_data.csv`（1.2万条，新增） | DoubleML分析 | 打标规则本身是混杂因素（见3.4节详解） |

**DiD数据里一个容易踩坑的技术细节，值得记住**：`pd.qcut(acct_value, 4, labels=list(reversed(tier_labels)))`——`qcut`默认把`labels[0]`分给数值最小的那一组，所以如果你想让"tier1"对应最高价值，必须把labels列表反过来传。这是我自己调试时踩过的坑，如果被问到"分层怎么做的"，可以提一句这个细节，显得你真的写过这段代码，不是复制的。

**为什么`did_data`里要给每个tier一个基线固定效应（`tier_effect = -0.25*(3-tier_rank[t])`）**：这不是随便加的噪声，而是刻意制造一个"高价值账户基线流失率本来就更低"的混杂结构——这样naive TWFE和clean-control DiD算出来的数字才会有真实的差异，DiD设计的教学意义才立得住。如果各tier基线完全一样，naive方法不会出现偏差，也就没什么好讲的了。

**`true_effect_prob`列——一个容易被忽略但很重要的字段**：
```python
true_effect_prob = sigmoid(final_logit) - sigmoid(baseline_logit)
```
这一列在DiD和dormant数据里都有，直接、精确地记录了"这一行数据的真实因果效应是多少"（概率尺度）。它存在的意义：让`05_did_analysis.py`和`07_doubleml_dormant.py`里的"grade against ground truth"能精确对照，而不是用近似值去估。（早期版本用的是"never-treated组按月份匹配的平均值"去近似真值，后来发现这个近似在tier间基线差异变大后会失真，所以改成了在数据生成时就精确算出来存下来——这是一个真实遇到并修复的bug，如果被问"怎么验证你的grading本身是对的"，这是个很好的例子。）

---

### 3.2 `02_validate_design.py` —— 在信任任何因果估计之前，先验证"假设"本身站不站得住

这是整个项目里最容易被低估、但其实最重要的一步：**任何准实验方法都建立在一个不能被数据本身证明、只能被间接检验的假设上。这一步就是做那个间接检验。**

**RDD的假设：running variable在cutoff处不能被人为操纵**
- 检验方法：McCrary-style密度检验——如果客户能精确控制取款金额卡在30%上下来"躲开"或"触发"RM联系，running variable的密度在30%处会出现跳跃
- 结果：`p = 0.151`（不显著，PASS）
- **为什么这个检验重要**：如果这个检验failed，意味着断点两侧的账户可能因为"自我选择"而不可比，RDD整个估计就不可信了。这一步不是走过场，是RDD能不能用的前提条件。

**DiD的假设：处理之前，各分层要"平行趋势"**
- 检验方法：限定在"所有分层都还没上线"的共同时间窗口内（也就是month 8之前，因为tier1最早在month 8上线），对"分层×日历月"交互项做F检验
- 结果：`F=0.115, p=0.951`（不显著，PASS）
- **一个容易被问到的设计细节**：为什么要限定"共同窗口"，而不是每个分层各自完整的"尚未处理"历史？因为如果tier4（从未上线）有24个月数据、tier1只有8个月pre-period数据，直接比较会因为数据量不对等而产生虚假的"趋势不同"结论——这跟真正要检验的假设（处理前是否平行）没有关系，纯粹是统计artifact。这是一个刻意的方法论选择，不是偷懒。

---

### 3.3 `03_predictive_model.py` —— 预测层：这账户会往哪个方向流失

- XGBoost多分类模型，预测目标：large_withdrawal / dd_stop / dormant / none 四选一
- ROC-AUC 0.65-0.72（每个类别one-vs-rest），top decile lift 2.8x-3.2x
- **为什么不追求0.95的AUC，也不用hard分类accuracy评估**：流失本身很稀有（合计约11%），而且是人的行为决定的，天生有噪声，这不是一个能做到0.95 AUC的问题。而且如果对一个89%都是"none"的目标做平衡权重的hard分类，precision数字会很难看但又不反映模型真实的使用方式——实际生产中是"按分数排序，用capacity能覆盖的量去打" ，不是"卡一个阈值做0/1分类"，所以用ranking质量（AUC、lift）评估更贴近真实用法。
- Feature importance显示region的重要性接近噪声水平——这是个很好的sanity check：说明模型没有学到"伪地理效应"。

---

### 3.4 三种因果推断方法：为什么是这三种，为什么每个triggers配一种不同方法

**这是整个项目设计上最核心的一个决策，一定要能讲清楚：三个触发器，结构完全不同，所以不能用同一个方法。**

#### 方法一：`04_rdd_analysis.py` —— 大额取款触发（有明确的cutoff）

- **场景**：取款超过余额30%时，RM主动联系
- **为什么适合RDD**：30%是一个确定性规则（不是随机分配），而且取款金额是连续变量，"卡在cutoff附近"这件事，两侧账户理论上除了是否被联系，其他特征应该差不多——这正是RDD的适用条件
- **怎么做**：`rdrobust`包，MSE-optimal bandwidth自动选择（Calonico-Cattaneo-Titiunik方法），报告conventional和bias-corrected robust估计（该信的是robust那个）
- **关键数字**：naive对比 -1.5pp（有偏，因为取款越大本来风险越高，这个混杂大致抵消了RM联系的真实负效应）→ RDD稳健估计 -10.7pp [95% CI -14.8, -6.6]，p<0.001 → 距离真实注入效应只差1.3pp
- **bandwidth敏感性**：h从4扫到20个月，估计值稳定在-9.4pp到-10.7pp之间，证明不是某一个bandwidth选择的巧合

#### 方法二：`05_did_analysis.py` —— DD停用offer（有分批上线，但没有单一cutoff）

- **场景**：$100的offer分批推给不同客户价值分层
- **为什么适合DiD而不是RDD**：这里没有一个连续的running variable和cutoff，而是"整批账户在某个时间点被处理"——这是DiD的典型场景，尤其是staggered adoption（不同分层不同时间上线）
- **为什么要按客户价值分层，而不是按地区**：这是我主动重新设计的部分——原来的版本是按地区分批，改成按价值分层之后：① 更贴近真实业务逻辑（RM/运营产能有限，先给高价值客户上线是真实会发生的推进节奏，比地区随意排序更合理）② 混杂结构更清晰：价值分层同时和"基线流失风险"、"处理时间"相关，这正是staggered adoption下naive TWFE产生偏差（Goodman-Bacon 2021分解）的经典结构，教学演示效果更好
- **两个估计量的对比，以及为什么不叫它"Callaway-Sant'Anna"**：
  - naive TWFE（一个大回归，所有tier和月份放一起，`C(value_tier)`固定效应）：-3.5pp
  - **clean-control（干净对照）DiD**：对每个上线分层，只用"当时还没上线"的分层做对照组，按post-period样本量加权汇总——这抓住了Callaway-Sant'Anna同样的核心直觉（绝不让已处理单位混进对照组），但**没有CS完整的doubly-robust估计和multiplier-bootstrap推断，所以我不把它叫CS，而叫它clean-control DiD**。这是刻意的措辞选择：宁可说一个我能完全解释、完全站得住的简化版本，也不要冒用一个我答不出细节的专有名词
  - -3.7pp，比naive的-3.5pp更接近真实值-4.7pp（差距不算戏剧化，但方向是对的，这个诚实的表述本身就是回答"为什么要做这个检验"的最好答案：你不能假设naive TWFE没问题，规模变大、异质性变强时这个偏差会更明显）
- **event study图讲的是什么**：效应不是上线当月就拉满，而是大约3个月后才爬坡到位（RM意识和客户接受度build up的过程）——这也是为什么第14节里的建议3会说"不要在month 1就评估一个新上线波次"

#### 方法三：`07_doubleml_dormant.py` —— 休眠账户再激活（既没cutoff也没分批上线）

**这是三个方法里最"弱"、但也是最能体现你分析深度的一个，一定要讲透。**

- **场景**：休眠信号触发提醒（账户被打标后一起处理，没有阈值可比，没有分批上线可比）
- **为什么RDD和DiD都用不上**：打标规则是`flagged = (dormancy_streak>=3) | ((dormancy_streak in [1,2]) & (engagement_score<25))`——这是一个分支/阈值规则，触发和处理是同一批账户、同一时间，没有"边界两侧对比"也没有"先后上线对比"可用
- **打标规则本身就是混杂因素——这是理解DoubleML为什么必要的关键**："confounding by indication"：最容易被打标的账户，本来也是最容易流失的账户。如果直接比较"被打标+处理"vs"没被打标"，你测的是"这两群人本来就不一样"，不是"处理有没有效果"
- **证据链，这是最有说服力的部分——三个方法并排对比**：
  - naive对比：**+11.2pp**（符号是反的！意味着处理反而"增加"了坏结果——纯粹是混杂造成的假象）
  - logistic回归调整（线性、加性协变量）：**+0.8pp**（符号还是反的，几乎没有调整效果）
  - **为什么logistic救不回来**：真实的混杂关系是一个阈值/分支规则（`dormancy_streak>=3`这种"或"逻辑），线性可加模型没法表达这种结构，但树模型（XGBoost）可以
  - **DoubleML（IRM，XGBoost做nuisance函数，交叉拟合，Neyman正交得分）：-6.5pp**，95% CI [-8.6, -4.5]，方向对了，而且离真值-9.8pp最近
- **为什么目标是ATT不是ATE**：真实注入的效应集中在被打标的这~30%账户里，全体平均效应（ATE）很小、很难精确估计；而"在实际会被处理的这批人身上"的效应（ATT）才是决定"要不要继续对这批人跑这个动作"的关键数字——这是一个统计概念要能讲清楚区别的地方
- **诚实的弱点，一定要主动提**：DoubleML的有效性依赖"selection-on-observables"——即"我们观测到了所有重要的混杂因素"，这个假设**不像RDD的操纵检验、DiD的平行趋势检验那样能被数据直接检验**。这也是为什么它在后面优化层和业务建议里被标注为"moderate confidence"，而不是跟RDD/DiD一样的"high confidence"。

---

### 3.5 `06_optimization.py` —— 把三个效应大小变成一个可执行的分配方案

- **问题的本质**：这是一个"multiple-choice knapsack"（多选择背包问题）——每个账户最多接受三种干预中的一种，同时受两个独立约束：总预算（$150,000）和RM产能（400个联系名额）。RM产能之所以要单独建模，是因为对"大额取款"这个干预来说，真正稀缺的是RM的时间，不是钱。
- **为什么不能用简单排序**：如果只有一个资源约束，排序就够了。但这里两个约束同时作用在不同类型的干预上（有些干预只花钱不占RM名额），简单排序找不到真正最优解，必须用正式的整数规划（PuLP + CBC solver）
- **目标函数**：`P(该流失模式) × 因果效应大小(pp) × account_value(风险敞口$) − 干预成本`，对所有被选中的"账户-干预"组合求和取最大
- **三个效应大小怎么来的，这是这一版重新设计后最大的进步**：
  - `large_withdrawal`: 10.7pp（来自RDD，high confidence）
  - `dd_stop`: 3.7pp（来自clean-control DiD，high confidence）
  - `dormant`: 6.5pp（来自DoubleML，moderate confidence）——**这三个数字现在全都来自真实跑出来的估计脚本，不再是硬编码的假设值**（早期版本dormant是拍脑袋的3pp"ASSUMED"，现在换成了DoubleML算出来的数字，并且带着自己的置信度标签一起传进优化器）
- **结果**：同样预算和RM产能下，优化器比"前50%价值"经验规则多保护63.2%的净价值（$300,187 vs $183,976，每万个评分账户）
- **一个值得主动讲的细节**：优化器只用了297/400个RM名额，没有把预算和名额花到最后一分——因为它在某个账户的边际预期价值转负时就会停止分配。这是经验规则没有的"资金纪律"，也是一个值得跟业务方讲的发现（说明原来的经验规则可能存在无谓的过度投入）。

---

### 3.6 `08_business_impact.py` —— 所有技术工作最终要落到这里

**这是这次任务里用户明确要求的落脚点：不能只是技术展示，必须变成"actionable insights and recommendations in support of enterprise-wide strategic business initiatives"，并且要评估impact。**

**headline impact：为什么按每万个账户折算，而不是编一个全行总数**
- 这个项目的数据是一个按比例缩小的demo（1万个账户的评分测试集），不是机构真实账本
- 编一个"全行regard xxx亿美元"的数字会显得更唬人，但这恰恰是这个项目从头到尾一直在刻意避免的"过度声称"
- 诚实的做法：报告增量价值 **+$116,211（+63.2%）每万个评分账户**，并明确给出放大规则："乘以（你真实的评分账户数/10,000）"——这样既没有编数字，又保留了实际可用性
- **真正值得强调的论点**：这个提升背后的机制（同样预算、同样RM产能、更好的targeting）会随账本规模线性放大，不像一次性固定成本的项目——这是比"多大的数字"更有说服力的business case

**敏感性检验：直接回答"你那个15%怎么定的"这个问题**
- 把account_value公式里的15%换成10%或20%，重新计算排序：Spearman秩相关 > 0.999，前25%价值分位账户98%+保持不变
- **这就是"这个假设不是load-bearing"的证明**，而不是空口说"我觉得15%合理"

**四条建议，按置信度分层——这是整个项目里最直接体现"business translation"能力的部分**：

| 优先级 | 建议 | 置信度 | 为什么这么排 |
|---|---|---|---|
| 1 | 用ILP优化器替代"前50%价值"经验规则 | 高——机械性改进，不是新的因果主张 | 风险最低，收益立即可见 |
| 2 | 把30%取款→RM联系正式化为受监控的政策 | 高——RDD，操纵检验直接测过 | 有设计层面的验证支撑 |
| 3 | 停DD offer上线后至少3个月再评估效果 | 高——DiD，平行趋势直接测过 | 防止"过早判死刑"一个正在起效的项目 |
| 4 | **不要**仅凭这次的数字扩大dormant play预算 | 中——DoubleML的假设无法直接检验 | 效应第二大，但故意排最后 |

**这张表最值得讲的一点**：优先级顺序是按"置信度"排的，不是按"效应大小"排的——dormant play的6.5pp其实是第二大的效应，但因为它的识别假设是唯一没法被数据直接验证的，所以被排到最后，并且明确写"不要扩大预算"。**这正是这个岗位想看到的判断力：知道什么时候一个数字"可信到能拿去做决策"，什么时候只是"值得进一步验证的信号"。**

---

## 3.5 生产化改造：从demo到production-ready

这一层是后加的，动机很直接：面试官问过"这只是个demo，如果要往生产走你会加什么"，标准答案是"加单元测试、把硬编码常量拆成配置文件、加数据校验和日志、把关键步骤全部seed保证完全可复现、再包一层服务接口"——与其只是嘴上这么说，不如真的把这六件事做出来，让项目本身就是这句话的证据。

**1) `config.py`——统一配置**
之前SEED、PRODUCT_VALUE_UPLIFT、BUDGET、RM_CAPACITY这些常量散落在好几个脚本里，各自硬编码一份。风险在于：改一个业务假设（比如下个月的预算）要去代码里找、还可能漏改某一处；而且没有一个地方能一眼看清"这次跑用的是什么配置"。现在全部集中到`config.py`，每个脚本从这一个地方导入——改配置就是改一行，不是改代码。

**2) `validation.py`——"fail loud and early"**
以前每个脚本都默认相信自己的输入是对的。这次加校验层之后，实际抓到了一个真实的边界bug：`01_generate_data.py`生成RDD数据时，`treated`是从**未四舍五入**的withdrawal_pct算的，但存到CSV里的withdrawal_pct是四舍五入到2位小数的——极少数边界行（比如原始值29.996）四舍五入后变成30.00，被验证层判定为"看起来该是treated=1，但实际标的是0"，这就不再是一个严格的sharp RDD了。修复方式是先四舍五入再判定treated，保证落盘的running variable和落盘的treatment flag永远一致。这是校验层真正抓到问题的例子，不是摆设。

**3) `logging_setup.py`——加日志，但不替换print**
每个脚本原本的print()叙事是特意保留的——面试展示的时候，终端上滚动的中文/英文讲解本身就是demo的一部分，不应该被log替换掉。但print()解决不了"我想看过去30天所有跑失败的记录"这种需求，所以logging是**加在旁边**的：console+`logs/<script>.log`，只在关键节点（开始/结束/校验失败/异常）记录，不是逐行替换。

**4) 完全可复现——这是最有技术含量的一部分**
之前只对`01_generate_data.py`做了seed（`np.random.default_rng(42)`），下游的XGBoost和DoubleML没有完全锁定，导致同一份代码跑两次数字会有大约1个百分点的漂移。深挖原因发现：**numpy有两套随机数系统**——新的Generator API（`np.random.default_rng`）和旧的全局RandomState（`np.random.seed`）。scikit-learn的`KFold(shuffle=True)`、以及DoubleML内部做cross-fitting时的fold划分，走的是**旧的全局RandomState**，而这个项目里只seed了新的Generator API，旧的那套从来没被锁定过——这才是DoubleML估计结果每次跑都不一样的真正原因，而不是"DoubleML本来就有随机性没法完全复现"。
修复：`config.seed_everything(seed)`同时锁定两套系统，在每个脚本的`if __name__ == "__main__":`最开头调一次。另外XGBoost即使给了`random_state`，多线程训练时histogram梯度求和的浮点数加法顺序不保证完全一致，也会带来微小漂移，所以把`n_jobs`固定为1（牺牲一点训练速度换取完全确定性，demo项目这个取舍是值得的）。
**验证方式**：把整个8步pipeline连续跑两次，用`diff -rq`比较`data/`、`output/`、`figures/`三个目录——完全没有任何差异，包括图片文件的字节级比较。这比"我觉得应该可复现"要有说服力得多。

**5) `pytest`单元测试**
脚本文件名是`01_generate_data.py`这种数字开头的命名（方便人一眼看出pipeline顺序），但数字开头不是合法的Python标识符，`import 01_generate_data`会直接语法错误。测试用`importlib.util.spec_from_file_location`按文件路径加载，而不是用`import`语句——`tests/conftest.py`里的`load_module()`就是干这个的。
测试覆盖：`config.py`的公式和seed行为、`validation.py`每个校验函数的通过/失败路径（包括上面提到的sharp RDD边界bug）、`01`生成的四个数据集是否满足自己声称的不变量（比如DiD必须正好4个value tier）、`06_optimization.py`的ILP在小规模合成输入下是否真的遵守预算/RM产能约束、以及FastAPI服务的几个端点。一共38+个测试，全部通过。
顺便也把`06_optimization.py`从一个**没有`if __name__ == "__main__":`保护、纯顶层代码**的脚本，重构成了`run_optimization()`函数——之前只要`import`这个文件就会触发整个优化流程当作副作用运行，根本没法被测试或被服务层复用。

**6) `service.py`——FastAPI服务层**
这一层刻意没有做成"每次请求都重新训练模型"的重量级服务——那是另一种系统。做的是把pipeline已经算好的结果（预测分数、优化结果、分级建议）通过HTTP暴露出来，加一个`/optimize`端点允许调用方传入不同的预算/RM产能实时重新求解ILP（因为ILP求解是亚秒级的，不是重新训模型，同步返回是合理的）。这个"批处理pipeline在后台跑、服务层只出结果+做轻量重算"的架构本身就是一个值得在面试里讲的设计判断，而不是随便加个Flask就完事。

**如果面试官追问"你怎么知道这些改动是有效的，不是走个形式"**：
- 用`validation.py`真的抓到了一个之前存在的边界bug（sharp RDD assignment rule的四舍五入不一致），这是校验层的直接证据；
- 用两次独立完整pipeline跑的`diff -rq`结果字节级一致，这是可复现性的直接证据；
- 用`pytest`跑出来的38+个测试全部通过、且其中专门有一条测试是复现"legacy RandomState没被seed"这个bug本身，这是测试真的在测东西的证据。

---

## 4. 数据是怎么在脚本间流动的（面试官问"整个pipeline怎么跑起来"时用）

```
01_generate_data.py
  └─→ data/{predictive,rdd,did,dormant}_data.csv, ground_truth.json
        │
        ├─→ 02_validate_design.py  （检验RDD/DiD假设，输出figures，不阻塞后续）
        │
        ├─→ 03_predictive_model.py → output/predictive_scores.csv
        │
        ├─→ 04_rdd_analysis.py → output/rdd_summary.json
        ├─→ 05_did_analysis.py → output/did_summary.json
        ├─→ 07_doubleml_dormant.py → output/doubleml_summary.json
        │        │
        │        ▼（06必须在07之后跑，因为它要读doubleml_summary.json）
        └─→ 06_optimization.py（读取03/04/05/07的所有输出）→ output/optimization_summary.json
                 │
                 ▼
        08_business_impact.py（读取06和03的输出）→ output/business_impact_summary.json
```

**一个容易被问到的细节**：为什么06要在07之后跑？因为优化器的dormant效应输入来自DoubleML的估计结果，如果顺序反了，06会读到一个不存在或过期的`doubleml_summary.json`。这也是为什么README里专门写了运行顺序（01→02→03→04→05→**07**→**06**→08）。

---

## 5. 20分钟讲解建议节奏（对应deck的18张slide）

1. **Title + Situation（slide 1-2）**：30秒，讲真实项目背景
2. **Task/Framing（slide 3）**：1分钟，务必讲清楚"哪些是真实的、哪些是这次的技术展示"——这是全场最重要的一句话，不要跳过
3. **Architecture（slide 4）**：1分钟，讲四层结构
4. **Predict（slide 5）**：1分钟，快速带过
5. **Explain-Causal 总览 + RDD + DiD + DoubleML（slide 6-11）**：8-9分钟，这是主体，每个方法讲"为什么用这个方法"+"关键数字"+"怎么验证的"
6. **Optimize（slide 12-13）**：3分钟
7. **Business Impact + Recommendations（slide 14-15）**：3分钟，这是收尾的高潮，一定要讲"按置信度排优先级"这个点
8. **Tying back + Limitations（slide 16-17）**：2分钟，主动暴露局限性，不要等被问
9. **Closing（slide 18）**：留时间给问题

---

## 6. 面试深挖高频考点快查（详细版在 `output/项目二-延伸-RDD-DiD-优化-QA.docx`，这里是精简记忆版）

| 如果被问… | 一句话答案骨架 |
|---|---|
| 这是真的假的？ | 项目和A/B结果是真的，RDD/DiD/DoubleML/优化/业务影响是后来用合成数据做的技术展示，坦诚说明 |
| RDD识别假设是什么？ | 30%这个断点上没有别的东西同时跳变；McCrary密度检验p=0.151验证过 |
| bandwidth怎么选？ | rdrobust的MSE-optimal自动选择，4-20个月敏感性检验都稳定在-9.4~-10.7pp |
| 为什么按价值分层不按地区？ | RM产能是真正的约束，更贴近现实；同时是staggered bias更干净的教学案例 |
| 这是Callaway-Sant'Anna吗？ | 不是，是简化的clean-control版本，我能完整解释每一步；生产环境会用`csdid`包 |
| 平行趋势怎么验证？ | 限定共同pre-period窗口做F检验，p=0.951 |
| 15%这个数字怎么定的？ | 敏感性检验：换成10%/20%排序几乎不变（Spearman>0.999） |
| 为什么dormant用DoubleML？ | 打标规则本身是混杂（confounding by indication），naive和logistic都得出错误符号 |
| 为什么ATT不是ATE？ | 真实效应集中在被打标的~30%账户里，ATT才是决策相关的数字 |
| 你怎么知道DoubleML真的有用？ | 和naive(+11.2pp错误符号)、logistic(+0.8pp错误符号)并排对比，只有DoubleML符号对(-6.5pp) |
| 为什么用ILP不用排序？ | 两个独立约束（预算+RM产能）同时作用在不同干预类型上 |
| 业务影响为什么不给全行大数字？ | 数据是缩小demo，编大数字是过度声称；给per-10000账户+明确放大规则更诚实也更有用 |
| 生产落地还差什么？ | SR 11-7独立模型风险验证；dormant play需要真实随机化小规模试验 |
| 这个项目做到production-ready了吗？ | 加了config统一常量、validation层（真抓到一个sharp RDD边界bug）、logging、两套numpy RNG都seed（修了DoubleML cross-fitting没被seed的漂移问题，两次完整跑diff结果字节级一致）、38+条pytest、FastAPI服务层——细节见本文档3.5节 |

---

## 7. 自测清单（合上文档，看你能不能不看数字答出这些）

- [ ] RDD naive vs robust estimate 分别是多少？（-1.5pp / -10.7pp）
- [ ] DiD naive TWFE vs clean-control 分别是多少？真值是多少？（-3.5 / -3.7 / 真值-4.7pp）
- [ ] Dormant三种方法的符号和数值？（naive +11.2错、logistic +0.8错、DoubleML -6.5对，真值-9.8）
- [ ] 优化结果的核心数字？（+63.2%，$300,187 vs $183,976）
- [ ] 业务影响headline数字和为什么这么算？（+$116,211每万账户，因为不能编全行总数）
- [ ] 四条建议的优先级顺序，以及为什么dormant排最后？（按置信度不按效应大小）
- [ ] 三个triggers为什么配三种不同方法？（结构不同：有cutoff用RDD，有分批上线用DiD，两者都没有用DoubleML）
- [ ] account_value公式怎么设计的，15%怎么辩护？（美元化+敏感性检验）
- [ ] DoubleML之前为什么会有run-to-run漂移，怎么修的？（cross-fitting fold划分走的是numpy legacy全局RandomState，之前没被seed；`config.seed_everything()`同时锁定两套RNG系统）
- [ ] 怎么证明现在完全可复现？（两次独立跑完整pipeline，`data/`、`output/`、`figures/`三个目录`diff -rq`零差异）
- [ ] 05为什么不叫Callaway-Sant'Anna？（简化版，没有doubly-robust和bootstrap，但核心直觉一致）
- [ ] 生产落地还缺什么？（独立模型风险验证 + dormant需要真实随机化试验）

如果这10条你都能脱口而出、并且能往下多讲两句"为什么"，这个项目就已经吃透了，可以去mock了。

---

## 8. 仓库文件地图（GitHub: crystal1361/vanguard-dda-retention-demo）

```
src/       01-08 号脚本，按顺序读一遍代码注释，本文档的3.1-3.6节和它逐一对应
           config.py / validation.py / logging_setup.py / service.py —— 生产化改造
           四件套，对应本文档第3.5节
tests/     pytest单元测试，覆盖config/validation/数据生成/优化器/服务层
data/      四份合成数据 + ground_truth.json（真实注入效应，面试官不会看，但你自己复习時有用）
figures/   9张图，deck里用的都是这里生成的
output/    每个脚本的数字结果（JSON/CSV）+ 这份QA docx
logs/      每个脚本的结构化日志（logging_setup.py写的）
deck/      build_deck.js（18页PPT）+ build_doc.js（生成QA docx的脚本）
README.md  仓库总览，运行顺序、测试、服务层的启动方式都在这里
```

准备mock的时候，建议开着这份文档 + deck，我来扮演面试官出题，你先答、我再挑毛病——随时说"开始mock"。
